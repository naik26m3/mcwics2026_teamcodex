# Railway Backend Configuration

# This file is not strictly required by Railway, but it documents the expected setup.
# Railway will auto-detect a Python project and run the start command below.

PORT=8000
PYTHONUNBUFFERED=1

# Start command for Railway
# Railway injects $PORT as an environment variable
# Command: python -m uvicorn main:app --host 0.0.0.0 --port $PORT

# Environment variables you MUST set in Railway dashboard (Variables tab):
# - MONGO_URI: MongoDB Atlas connection string
# - GEMINI_API_KEY: Google Gemini API key
# - ALLOWED_ORIGINS: Comma-separated list of frontend origins (e.g., https://your-app.vercel.app)

# Example ALLOWED_ORIGINS:
# https://mcwics2026-teamcodex.vercel.app,https://mcwics2026-preview.vercel.app
